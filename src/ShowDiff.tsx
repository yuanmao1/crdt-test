import React from'react';

export const ShowDiff = ({

}) => {

    return (
        <div style={{ border: '2px solid black', width: '30%', height: '90%' }}>
            ShowDiff
        </div>
    );
};